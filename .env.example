# Database connection string
# For local dev (Docker): postgresql://postgres:postgres@localhost:5432/superlista
# For Railway: auto-populated by the PostgreSQL plugin
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/superlista

# Node environment
NODE_ENV=development

# Port (Railway sets this automatically)
PORT=3000
